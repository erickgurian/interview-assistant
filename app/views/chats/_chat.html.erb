<div id="<%= dom_id chat %>" class="flex w-full sm:w-auto my-5 space-y-3">
  <div>
    <strong class="block font-medium mb-1"><%= chat.messages.second&.content || chat.messages.first.content %></strong>
  </div>

  <div style="">
    <%= time_ago_in_words(chat.created_at) %>
  </div>
</div>
